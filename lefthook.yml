# pre-commit:
#   parallel: true
#   commands:
#     lint:
#       glob: '*.{js,jsx,ts,tsx}'
#       run: npm run lint
    

commit-msg:
  commands:
    commitlint:
      run: npx commitlint --edit {1}
      
# pre-push:
#   parallel: true
#   commands:
#     test:
#       run: npm run test